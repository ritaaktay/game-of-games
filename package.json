{
  "name": "squid-game",
  "version": "1.0.0",
  "description": "",
  "main": "bundle.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild index.js --outfile=bundle.js --external:nod_modules --platform=node --bundle --watch",
    "start": "http-server -c-1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.12",
    "http-server": "^14.1.1",
    "jest": "^29.2.2",
    "jest-environment-jsdom": "^29.2.2",
    "nodemon": "^2.0.20"
  },
  "jest": {
    "collectCoverageFrom": [
      "lib/{!(canvasDisplay),}.js"
    ]
  }
}
